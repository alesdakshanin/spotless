{
  "name": "spotless",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Find and replace unplayable songs in your Spotify library",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0",
    "vitest": "^3.0.0"
  }
}
